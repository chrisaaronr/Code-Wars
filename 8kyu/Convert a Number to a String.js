//CONVERT A NUMBER TO A STRING!

// We need a function that can transform a number into a string.

// In what ways can you achieve that?

// SOLUTION

function numberToString(num) {
    return String(num)
}

function numberToString(num) {
    return num.toString();
  }

  const numberToString = num => String(num);